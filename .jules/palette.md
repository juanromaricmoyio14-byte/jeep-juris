## 2025-05-29 - Loader state for async submission
**Learning:** Added `Loader2` from `lucide-react` to `login.tsx` for the submit button to provide visual feedback during async operation. This prevents user confusion and multiple clicks. Using `flex items-center justify-center` keeps it aligned nicely.
**Action:** Consistently ensure buttons that trigger async actions use a loading indicator in this repository.
